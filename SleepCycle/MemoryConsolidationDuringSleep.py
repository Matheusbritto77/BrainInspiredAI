# Consolidação de memória durante o sono
def consolidate_memory(memory_data):
    print(f"Consolidando memória: {memory_data}")

if __name__ == '__main__':
    consolidate_memory('Memória consolidada durante o sono')
